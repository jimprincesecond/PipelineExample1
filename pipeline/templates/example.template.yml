parameters:
  env: ''
  webserver: ''
  jobs:
  - deployment: Deploy${{parameters.env}}
    displayName: Deploy${{parameters.env}}

    variables: 
    - group: hangfiredashboard-${{ parameters.env }}
    - name: appserverIp
      value: webserverIp$(webserver)

    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
          - script: echo envname = ${{parameters.env}}
          - script: echo deploying to ip $(appserverIp)



